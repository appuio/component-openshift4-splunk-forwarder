parameters:
  kapitan:
    compile:
      - input_paths:
          - openshift4-splunk-forwarder/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - openshift4-splunk-forwarder/component/main.jsonnet
        input_type: jsonnet
        output_path: openshift4-splunk-forwarder/
